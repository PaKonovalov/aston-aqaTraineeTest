Примитивы внутри себя хранят значения, а ссылочные ссылки на объекты

Примитивы хранятся в сткеке, ссылки в куче

    - целые числа (byte, short, int, long);
    - числа с плавающей точкой (float, double);
    - логический (boolean);
    - символьный (char).