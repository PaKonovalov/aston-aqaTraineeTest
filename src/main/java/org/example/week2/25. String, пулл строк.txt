Расположен в Heap памяти

Хранит одинаковые строки в одних и техже объектах для экономии памяти

Метод .intern() вручную выполнить интернирование строки в пул путем вызова метода intern() у объекта типа String

String str3 = (new String("TopJava")).intern();
